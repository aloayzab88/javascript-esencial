// * Bucle for
let x = 0;

for (let contador = 1; contador <=10; contador++) {
    console.log(contador);
}

for (let contador = 1, lista = []; contador <=10; contador++) {
    lista.push(contador);
    console.log(lista);
}

for (let contador = 100; contador >= 1 ; contador--) {
    console.log(contador);    
}